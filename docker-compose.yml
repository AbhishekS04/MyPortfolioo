version: '3.8'

services:
  web:
    build: .
    ports:
      - '3000:3000'
    environment:
      - NODE_ENV=production
    # Uncomment the below lines if you want to pass your local environment variables to the container
    env_file:
      - .env.local
